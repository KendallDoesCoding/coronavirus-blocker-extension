# ui

This is the path for the temp UI for this extension. It still needs a lot of working on.

# css
I used the same template as my other extensions for the UI so the css has a lot of not required variables. I need to get to deleting them as soon as possible.
